# Legado 用户协助调试

## 适用范围

本文档用于 **人工/App 复核阶段**，即 validator 已标记 `needs_app_review`、`validator_limitation` 或 `failed_unresolved` 后，需要用户在 Legado App 中配合调试的场景。

**validator 失败的第一处理流程是 AI 自动回修**（见 `validation-policy.md`），不是本文档。只有当 AI 无法自动修复时，才进入本文档的调试流程。

## 使用约束

- 仅在以下情况进入调试模式：
  - validator 标记 `needs_app_review`（Cloudflare、验证码、登录、WebView、付费墙）
  - validator 标记 `validator_limitation`（工具不支持的规则能力）
  - validator 标记 `failed_unresolved`（3 次回修后仍未通过）
  - 用户反馈"书源用不了了"
  - 导入失败
  - 搜索、详情、目录、正文任一链路失效
  - Legado 在登录、调试或阅读过程中崩溃
- 先判定失败阶段，再索取证据。
- 单次回复只索取当前阶段的最小证据包，不提前索要全部截图和日志。
- 可要求文本源码时，优先要求文本源码。
- 可在 Legado 内复现的问题，不先要求外部抓包或系统级日志。
- 不要只写内部术语，必须写明用户实际点击路径和界面里的中文菜单名。
- 一旦用户给出运行失败反馈、报错截图或异常日志，必须立即切换到本调试流程，不得继续主流程分析。
- 禁止在未索取最小证据包前直接改规则。
- 禁止把本地文件推断优先于用户当前 App 内实际规则、调试输出和源码。

## 官方入口依据

- 书源编辑页右上角三点菜单包含：
  - `登录`
  - `调试源`
- 调试页输入框提示 "搜索书名、作者"，实际支持以下输入：
  - 搜索关键字（如 `系统`）
  - 详情页 URL（如 `https://m.qidian.com/book/1015609210`）
  - `++目录页URL`（调试目录）
  - `--正文页URL`（调试正文）
  - `标题::发现URL`（调试发现）
- 调试页右上角三点菜单包含：
  - `搜索源码`
  - `书籍源码`
  - `目录源码`
  - `正文源码`
  - `帮助`
- 调试页右上角扫码图标可直接扫描二维码导入调试 URL
- 关于页包含：
  - `崩溃日志`
  - `保存日志`

## 故障分流矩阵

| 失败阶段 | 典型症状 | 先让用户进入的位置 | 输入内容 | 最小证据包 |
| --- | --- | --- | --- | --- |
| 登录 | 登录后仍未生效、提示未登录、会员内容不可见 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `登录` | 无 | `loginUrl/loginUi/loginCheckJs` 截图、登录页截图、登录成功后页面截图 |
| 导入 | JSON 无法导入、导入时报字段错误、导入后书源未出现 | 导入界面 | 无 | 导入报错截图、`book-source.json` 原文、必要时书源列表截图 |
| 搜索 | 无结果、报错、结果字段错乱 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `调试源` | 搜索关键词 | 调试结果区截图、调试页右上角三点里的 `搜索源码` 原文、搜索规则截图 |
| 详情 | 能搜到书但详情打不开、字段空白、封面或简介异常 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `调试源` | 详情页 URL | 调试结果区截图、调试页右上角三点里的 `书籍源码` 原文、详情规则截图 |
| 目录 | 详情正常但目录打不开、章节不全、顺序错误、目录分页失败 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `调试源` | `++目录页URL` | 调试结果区截图、调试页右上角三点里的 `目录源码` 原文、目录规则截图 |
| 正文 | 正文空白、乱码、广告页、跳错页、仅首章正常 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `调试源` | `--正文页URL` | 调试结果区截图、调试页右上角三点里的 `正文源码` 原文、至少两个异常章节 URL、正文规则截图 |
| 发现 | 发现页打不开、分类错乱 | 书源管理 -> 对应书源 -> 编辑页 -> 右上角三点 -> `调试源` | `标题::发现URL` | 调试结果区截图、调试页能看到的对应源码文本、发现规则截图 |
| 崩溃 | 调试页、登录页、阅读页闪退 | 我的 -> 关于 -> `崩溃日志` 或 `保存日志` | 无 | 崩溃日志截图或文本、`logs.zip`、Legado 版本号 |

## 规则截图字段矩阵

| 失败阶段 | 必要字段 |
| --- | --- |
| 登录 | `loginUrl`、`loginUi`、`loginCheckJs` |
| 搜索 | `searchUrl`、`bookList`、`name`、`bookUrl` |
| 详情 | `init`、`name`、`author`、`intro`、`tocUrl` |
| 目录 | `chapterList`、`chapterName`、`chapterUrl`、`nextTocUrl` |
| 正文 | `content`、`title`、`nextContentUrl`、`webJs`、`sourceRegex`、`replaceRegex` |
| 发现 | `exploreUrl`、`ruleExplore` 相关字段 |

## 回复结构

每次要求用户配合时，固定按以下顺序输出：

1. 从哪里进入
2. 点哪个菜单
3. 输入什么
4. 调试完成后回传哪几项证据
5. 若涉及登录、验证码、短信、扫码或人工确认，明确要求用户先手动完成

## 可直接发送的话术模板

### 登录问题

```md
先不要继续改规则，先确认登录态。

请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `登录`

如果登录过程需要扫码、验证码、短信或人工确认，请你先手动完成。

完成后把下面 3 项发给我：
1. `loginUrl`、`loginUi`、`loginCheckJs` 的截图
2. 登录页截图
3. 登录成功后落地页面的截图
```

### 导入失败

```md
先定位是不是 JSON 本身导入失败。

请把下面 3 项发给我：
1. 导入时报错截图
2. 当前 `book-source.json` 原文
3. 如果导入后名称异常或书源没出现，再补一张书源列表截图
```

### 搜索失败

```md
请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `调试源`
5. 输入这个搜索词：`<关键词>`

调试完成后，把下面 3 项发给我：
1. 调试结果区截图
2. 在调试页点右上角三点，点 `搜索源码`，把里面的原文发给我
3. 回到书源编辑页，打开“搜索”标签，截一张包含 `searchUrl`、`bookList`、`name`、`bookUrl` 的截图
```

### 详情失败

```md
请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `调试源`
5. 输入这个详情页 URL：`<详情页URL>`

调试完成后，把下面 3 项发给我：
1. 调试结果区截图
2. 在调试页点右上角三点，点 `书籍源码`，把里面的原文发给我
3. 回到书源编辑页，打开“详情”标签，截一张包含 `init`、`name`、`author`、`intro`、`tocUrl` 的截图
```

### 目录失败

```md
请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `调试源`
5. 输入这个目录调试值：`++<目录页URL>`

调试完成后，把下面 3 项发给我：
1. 调试结果区截图
2. 在调试页点右上角三点，点 `目录源码`，把里面的原文发给我
3. 回到书源编辑页，打开“目录”标签，截一张包含 `chapterList`、`chapterName`、`chapterUrl`、`nextTocUrl` 的截图
```

### 正文失败

```md
请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `调试源`
5. 输入这个正文调试值：`--<正文页URL>`

调试完成后，把下面 4 项发给我：
1. 调试结果区截图
2. 在调试页点右上角三点，点 `正文源码`，把里面的原文发给我
3. 至少两个失败或异常章节 URL
4. 回到书源编辑页，打开“正文”标签，截一张包含 `content`、`title`、`nextContentUrl`、`webJs`、`sourceRegex`、`replaceRegex` 的截图
```

### 发现失败

```md
请在 Legado 里按这个路径操作：
1. 打开 `书源管理`
2. 找到这个书源，进入编辑页
3. 点右上角三点
4. 点 `调试源`
5. 输入这个发现调试值：`<标题>::<发现URL>`

调试完成后，把下面 3 项发给我：
1. 调试结果区截图
2. 在调试页点右上角三点，打开对应源码，把原文发给我
3. 回到书源编辑页，打开“发现”标签，把相关规则截图发给我
```

### 崩溃或异常中断

```md
这个不是先改规则，先取崩溃证据。

请在 Legado 里按这个路径操作：
1. 打开 `我的`
2. 打开 `关于`
3. 点 `崩溃日志`

然后把下面内容发给我：
1. 崩溃日志截图或文本
2. 如果你能导出日志，再回到 `关于` 页面点 `保存日志`，把生成的 `logs.zip` 发给我
3. 你当前使用的 Legado 版本号

如果还没开启日志记录，请先在设置里开启 `记录日志`，再复现一次并重新导出。
```

## 调试阶段附加规则

- 若用户只说“打不开”“没内容”“报错了”，先把问题压缩成以下之一：
  - 搜索失败
  - 详情失败
  - 目录失败
  - 正文失败
  - 登录失败
  - 导入失败
  - 崩溃
- 若站点需要登录，不要在未确认登录态前继续分析正文或目录异常。
- 若正文只首章正常，默认优先怀疑：
  - `nextContentUrl`
  - `content` 提取范围
  - `webJs`
  - `sourceRegex`
  - `replaceRegex`
- 若目录不全，默认优先怀疑：
  - `chapterList`
  - `nextTocUrl`
  - 目录页分页入口
- 若搜索结果错乱，默认优先怀疑：
  - `bookList`
  - `bookUrl`
  - 搜索页是否为异步接口或多模板结果页

## 官方依据

- `debugHelp.md`
- `source_edit.xml`
- `BookSourceEditActivity.kt`
- `book_source_debug.xml`
- `BookSourceDebugActivity.kt`
- `AboutFragment.kt`
